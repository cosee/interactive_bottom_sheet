name: interactive_bottom_sheet_example
description: Demonstrates how to use the interactive_bottom_sheet plugin.
publish_to: none
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  interactive_bottom_sheet:
    path: ../
  flutter_lorem: ^2.0.0
  flutter_map: ^5.0.0
  latlong2: ^0.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  cosee_lints: ^0.2.0

flutter:
  uses-material-design: true
